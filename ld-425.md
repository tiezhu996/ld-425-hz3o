<!-- 改编自：研究装修艺术主题项目提示词/ld-425/ld-425.md；后端由 NestJS+TypeScript 改造为 Go 1.22 + Gin + GORM -->

## 项目主要需求

### 项目概述

【全栈Web应用】家居装修全流程管理平台。面向业主、设计师和施工方，覆盖装修设计阶段、施工进度、材料清单、预算管控和3D户型预览。**本项目用于验证模型跨文件协同改动能力，禁止将多个职责合并进单一文件。**

### 核心实体（≥ 3 个，每个贯穿全栈）

1. **装修项目（RenovationProject）** — 字段：项目名称、户型（一室/两室/三室/四室/别墅/复式）、面积（m²）、装修风格（Modern/Chinese/Nordic/Japanese/Industrial/Mediterranean/Minimalist）、地址、业主ID、设计师ID、施工队长ID、项目状态（Designing/Quoting/InProgress/Completed/Archived）、合同金额、开工日期、预计竣工日期
   - 关联：1个项目 → 多个设计阶段、多个材料清单、多个预算项
   - **贯穿全栈**

2. **设计阶段（DesignPhase）** — 字段：所属项目ID、阶段名称（方案设计/效果图/施工图/软装方案）、设计师ID、阶段状态（NotStarted/InProgress/Revision/Approved）、版本号、设计说明、设计文件URL列表、审核意见、审核人ID
   - 业务动作：提交设计、业主审核（通过/驳回修改）、版本迭代
   - **贯穿全栈**

3. **材料清单（MaterialItem）** — 字段：所属项目ID、材料名称、品类（瓷砖/地板/油漆/灯具/卫浴/五金/板材/其他）、规格型号、品牌、数量、单位、单价、总价（自动计算）、采购状态（NotPurchased/Ordered/Delivered/Installed）、供应商、使用空间（客厅/卧室/厨房/卫生间/阳台）
   - **贯穿全栈**

4. **预算项（BudgetItem）** — 字段：所属项目ID、预算类别（Design/Material/Labor/Furniture/Appliance/Other）、预算金额、实际花费、差异金额（自动计算）、备注
   - **贯穿全栈**

5. **施工节点（ConstructionNode）** — 字段：所属项目ID、节点名称（拆改/水电/木工/瓦工/油漆/安装/软装）、计划开始/结束日期、实际开始/结束日期、状态（Pending/InProgress/Completed/Delayed）、验收状态（Pending/Passed/Failed）、验收照片URL列表、验收说明
   - **贯穿全栈**

### 核心页面（≥ 4 个）

1. **项目总览** `/dashboard` — 消费：RenovationProject + ConstructionNode + BudgetItem
   - 在建项目卡片列表、施工进度甘特图、预算执行率环形图、待验收节点提醒
   - 使用 `<StatusBadge>`, `<ProgressBar>`, `<AlertBanner>`

2. **设计管理** `/designs` — 消费：DesignPhase + RenovationProject
   - 设计阶段列表（按项目），版本历史对比，提交审核/审核操作
   - 使用 `<StatusBadge>`, `<StepIndicator>`, `<VersionTag>`

3. **材料管理** `/materials` — 消费：MaterialItem + BudgetItem
   - 材料清单表格（按空间/品类筛选），采购进度跟踪，材料费用汇总
   - 使用 `<StatusBadge>`, `<ProgressTag>`, `<EmptyState>`

4. **施工进度** `/construction` — 消费：ConstructionNode + RenovationProject
   - 施工时间线（按节点排列），验收操作（上传照片+填写说明），延期节点高亮
   - 使用 `<StatusBadge>`, `<Timeline>`, `<StepIndicator>`

5. **预算管理** `/budget` — 消费：BudgetItem + MaterialItem
   - 预算分项列表（按类别），预算vs实际柱状图，超支预警
   - 使用 `<StatusBadge>`, `<StatCard>`, `<AlertBanner>`

### 横切关注点（≥ 2 个）

1. **RBAC 权限控制**
   - 角色：Admin / Designer / Contractor / Owner / ProjectManager
   - 触达文件层：DB role表 + RBAC中间件 + 路由守卫 + 按钮显隐（Owner只能审核不能编辑设计）+ 类型定义

2. **操作日志**
   - 记录设计提交/审核、材料采购变更、施工验收、预算调整
   - 触达文件层：DB audit_log表 + 日志中间件 + service埋点 + 前端操作时间线 + 类型定义

3. **全局异常处理**
   - 触达文件层：后端错误处理中间件 + handler异常 + 前端ErrorBoundary + API拦截器 + 错误码枚举

### 共享枚举/常量（≥ 2 组）

1. **装修风格 DecorStyle**：Modern / Chinese / Nordic / Japanese / Industrial / Mediterranean / Minimalist
   - 后端：`backend/internal/constants/enums.go`；前端：`frontend/src/types/enums.ts`

2. **项目阶段状态 PhaseStatus**：NotStarted / InProgress / Revision / Approved

3. **施工节点 ConstructionPhase**：Demolition / Plumbing / Carpentry / Tiling / Painting / Installation / SoftFurnishing

4. **采购状态 PurchaseStatus**：NotPurchased / Ordered / Delivered / Installed

### 共享前端组件清单（≥ 3 个）

1. **`<StatusBadge>`** — 所有页面
2. **`<ProgressBar>`** — 仪表盘、预算管理（预算执行率）
3. **`<Timeline>`** — 施工进度（节点时间线）
4. **`<StepIndicator>`** — 设计管理（多阶段流程）
5. **`<AlertBanner>`** — 仪表盘（延期/超支预警）

### 共享 hooks/utils 清单（≥ 2 个）

1. **`useProjectPhase`** — 封装项目阶段状态流转逻辑
2. **`usePagination`** — 通用分页
3. **`formatBudget`** — 预算金额格式化和差异计算

### 后端中间件清单（≥ 2 个）

1. **`AuthMiddleware`** — JWT 鉴权
2. **`RBACMiddleware`** — 角色校验
3. **`AuditLogMiddleware`** — 操作日志
4. **`ErrorHandlerMiddleware`** — 统一异常
5. **`RequestLoggerMiddleware`** — 请求日志
6. **`UploadMiddleware`** — 文件上传（设计文件、验收照片，Gin multipart/form-data）

### 技术要求

- **英文短名（用于 Docker Compose）**：`home-renovation`
- **前端端口**：18805 / 80
- **后端端口**：19305 / 8080
- **前端**：React 18 + TypeScript + Vite
- **UI 库**：Ant Design 5
- **图表**：ECharts
- **后端**：Go 1.22 + Gin + GORM
- **数据库**：MySQL 8.0
- **ORM**：GORM
- **认证**：JWT（使用 github.com/golang-jwt/jwt/v5）
- **状态管理**：Zustand
- **文件上传**：Gin multipart/form-data（设计文件、验收照片）
- **前端构建工具**：Vite
- **后端构建工具**：go build / go mod tidy

### 文件结构强制清单

**前端（严禁合并职责到单一文件）**：
```
frontend/src/
├── api/              # project.ts, design.ts, material.ts, budget.ts, construction.ts
├── stores/           # projectStore.ts, designStore.ts, materialStore.ts, budgetStore.ts, constructionStore.ts
├── types/            # index.ts, enums.ts, project.ts, design.ts, material.ts, budget.ts, construction.ts
├── components/common/  # StatusBadge, ProgressBar, Timeline, StepIndicator, AlertBanner, VersionTag, ProgressTag, EmptyState
├── hooks/            # useProjectPhase.ts, usePagination.ts
├── pages/            # Dashboard, DesignManage, MaterialManage, ConstructionProgress, BudgetManage
├── router/           # index.tsx, guards.tsx
├── utils/            # formatBudget.ts, request.ts, upload.ts
└── constants/        # apiPaths.ts, roomTypes.ts
```

**后端（严禁合并职责到单一文件）**：
```
backend/
├── go.mod
├── go.sum
├── Dockerfile
├── cmd/
│   └── server/
│       └── main.go
├── internal/
│   ├── config/       # database_config.go, jwt_config.go, upload_config.go
│   ├── model/        # project.go, design_phase.go, material_item.go, budget_item.go, construction_node.go
│   ├── repository/   # project_repository.go, design_repository.go, material_repository.go, budget_repository.go, construction_repository.go
│   ├── service/      # project_service.go, design_service.go, material_service.go, budget_service.go, construction_service.go
│   ├── handler/      # project_handler.go, design_handler.go, material_handler.go, budget_handler.go, construction_handler.go
│   ├── router/       # project_router.go, design_router.go, material_router.go, budget_router.go, construction_router.go
│   ├── middleware/   # auth.go, rbac.go, audit_log.go, error_handler.go, request_logger.go, upload.go
│   ├── dto/          # project_dto.go, design_dto.go, material_dto.go, budget_dto.go, construction_dto.go
│   ├── constants/    # enums.go, error_codes.go
│   ├── errors/       # business_error.go, validation_error.go
│   ├── utils/        # budget_calculator.go, response.go, upload.go
│   └── logger/       # logger.go
└── database/
    ├── migrations/
    └── seeds/
```

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。


### 部署要求

Docker Compose 一键部署。
- 顶层 `name: home-renovation`
- 不写 `version:` 字段
- MySQL 命名卷 + healthcheck
- 后端 depends_on service_healthy
- 后端提供 `GET /healthz` 健康检查接口，并配置 healthcheck
- 前端 `${FRONTEND_PORT:-18805}:80`，后端 `${BACKEND_PORT:-19305}:8080`
- 前端 Nginx `location /api/` 反向代理到 `http://backend:8080/`
- 后端 `backend/Dockerfile` 采用 Go 多阶段构建（golang:1.22-alpine → alpine:3.20）
- `.env` 必含：`COMPOSE_PROJECT_NAME=home-renovation`, `DB_NAME`, `DB_USER`, `DB_PASSWORD`, `DB_ROOT_PASSWORD`, `JWT_SECRET`, `FRONTEND_PORT`, `BACKEND_PORT`
- 支持 `docker compose up -d`（不需要 `-p`）

### backend/Dockerfile 参考（务必遵循）

```dockerfile
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

### 构建验证清单

- `backend/` 下执行 `go mod tidy` 无错误
- `backend/` 下执行 `go build ./...` 无错误
- 在中文目录名下执行 `docker compose config --quiet` 不报错
- 启动后 `docker compose ps` 显示全部容器 healthy
- 浏览器访问 18805 端口可打开页面，19305 端口后端 API 返回正常
- 文件上传（设计文件、验收照片）功能正常

### Git 要求

`git init` + `.gitignore`（至少包含 `node_modules/`、`dist/`、`.env`、`.DS_Store`、`*.log`、`.idea/`、`.vscode/`，以及 Go 相关 `bin/`、`backend/server`、`*.exe`、`*.test`、`coverage.out`）。

### README 写法

中文 README，Docker 启动最前、访问地址（18805/19305）、技术栈表格（后端写 Go 1.22 + Gin + GORM）、目录结构、枚举位置、License。
